/**
 * Polaris IDE backend module.
 *
 * Placeholder for future backend customization (custom endpoints, etc.).
 */

import { ContainerModule } from "@theia/core/shared/inversify";

export default new ContainerModule(() => {
  // Custom backend bindings go here.
});
